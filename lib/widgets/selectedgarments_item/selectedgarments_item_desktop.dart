part of selectedgarments_item_widget;

class _SelectedgarmentsItemDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('selectedgarments_item_desktop'),
    );
  }
}