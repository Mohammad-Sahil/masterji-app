part of order_item_widget;

class _OrderItemTablet extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('order_item_tablet'),
    );
  }
}