part of order_item_widget;

class _OrderItemDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('order_item_desktop'),
    );
  }
}